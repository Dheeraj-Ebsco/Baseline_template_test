terraform {
  backend "s3" {
    bucket         = "eis-shared-services-terraform-state"
    key            = "infrastructure/{{my_new_account}}.tfstate"
    region         = "us-east-1"
    encrypt        = true
    dynamodb_table = "eis-shared-services-terraform-state"
  }
}